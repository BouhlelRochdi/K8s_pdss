apiVersion: v1
kind: Service
metadata:
  name: pdss-front-service
  namespace: pdss-namespace-front
spec:
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: pdss-front
  type: ClusterIP #LoadBalancer
